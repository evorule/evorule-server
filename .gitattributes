# EvoRule .gitattributes
# 控制 git 换行符和文件类型处理

# TCB 宪法双仓关系:钉死 LF,消除行尾检出伪影。两份宪法自本仓 v0.4.1 起为
# 同源分叉(核心=最小评估集,本仓=最小集+会话桥接规则,见 CHANGELOG
# v0.4.1/v0.4.2),版本号与内容均不要求一致;核心最小集变更仍应人工合入本仓副本
resources/core_eval.json text eol=lf
